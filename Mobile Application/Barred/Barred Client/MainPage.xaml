<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Barred_Client.MainPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ContentPage.Behaviors>
        <toolkit:StatusBarBehavior StatusBarColor="{StaticResource Primary}" StatusBarStyle="LightContent" />
    </ContentPage.Behaviors>

    <Grid
        Margin="0,30,0,0"
        Padding="30"
        RowDefinitions="*,Auto,Auto"
        RowSpacing="15">

        <StackLayout Grid.Row="0" Spacing="25">
            <Image
                Aspect="AspectFit"
                Source="node_red_icon.png"
                WidthRequest="200" />

            <Label
                SemanticProperties.HeadingLevel="Level1"
                Style="{StaticResource Headline}"
                Text="BARRED" />

            <Label
                SemanticProperties.HeadingLevel="Level2"
                Style="{StaticResource SubHeadline}"
                Text="The Node RED Barcode Processing Platform" />
        </StackLayout>


        <Label
            FontSize="18"
            Grid.Row="1"
            HorizontalOptions="Center"
            HorizontalTextAlignment="Center"
            Text="This device has not been enrolled, please click below to enroll" />


        <Button
            Clicked="StartEnrollemnt"
            FontSize="20"
            Grid.Row="2"
            HorizontalOptions="Fill"
            Margin="0,10,0,20"
            Text="Enrol" />
    </Grid>
</ContentPage>


